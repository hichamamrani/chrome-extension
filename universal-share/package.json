{
  "name": "universal-share",
  "version": "1.0.0",
  "description": "A native mobile-style Share Sheet for Chrome.",
  "author": "Algobots",
  "license": "MIT",
  "scripts": {
    "generate:icons": "python3 scripts/generate-icons.py",
    "generate:assets": "node store-assets/generate-screenshots.mjs",
    "package": "node scripts/package-extension.mjs",
    "prepare:store": "npm run generate:icons && npm run generate:assets && npm run package"
  },
  "dependencies": {
    "puppeteer": "^24.43.1"
  }
}
